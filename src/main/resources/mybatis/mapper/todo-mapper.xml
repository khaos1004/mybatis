<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.project.mapper.TodoMapper">
    <insert id="save">
        insert into TODO(CONTENT, ISCOMPLETED)
        values (#{todo.content}, #{todo.isCompleted});
    </insert>

    <select id="sel" resultType="org.project.vo.Todo">
        SELECT * FROM TODO
    </select>

<!--    <select id="callYourProcedure" statementType="CALLABLE">-->
<!--        { CALL YourProcedure(#{param1, mode=IN, jdbcType=INTEGER}, #{param2, mode=OUT, jdbcType=VARCHAR}) }-->
<!--    </select>-->
</mapper>